<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="14dp"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="@string/label_stream_fallback"
        android:textColor="@color/text_secondary"
        android:textSize="16sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/btnPickStreamFallback"
            android:layout_width="80dp"
            android:layout_height="match_parent"
            android:layout_marginEnd="12dp"
            android:background="@drawable/picker_button_bg"
            android:contentDescription="@string/btn_pick_stream_fallback"
            android:nextFocusDown="@id/btnPickTrailerPrimary"
            android:nextFocusRight="@id/editStreamFallback"
            android:nextFocusUp="@id/btnPickStreamPrimary"
            android:src="@android:drawable/ic_menu_search"
            app:tint="@color/text_primary" />

        <EditText
            android:id="@+id/editStreamFallback"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/edit_text_bg"
            android:hint="@string/hint_stream_fallback"
            android:importantForAutofill="no"
            android:imeOptions="actionNext"
            android:inputType="text"
            android:maxLines="1"
            android:nextFocusDown="@id/editAdditionalAllowedHosts"
            android:nextFocusLeft="@id/btnPickStreamFallback"
            android:nextFocusRight="@id/btnPickTrailerFallback"
            android:nextFocusUp="@id/editStreamPrimary"
            android:paddingHorizontal="16dp"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_hint"
            android:textSize="18sp" />
    </LinearLayout>
</LinearLayout>
